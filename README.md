# Vendure Client

This is a client library for the [Vendure](https://vendure.io/) GraphQL API.

## Setup

Install the dependencies:

```bash
npm install
```

## Get started

Build the library:

```bash
npm run build
```